[project]
name = "lm-engine"
version = "0.1.0"
description = "lm-engine is a open source LLM training framework"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "datasets>=4.2.0",
    "parameterized>=0.9.0",
    "pre-commit>=4.3.0",
    "pydantic>=2.12.0",
    "pytest>=8.4.2",
    "torch>=2.8.0",
    "transformers>=4.57.0",
]
